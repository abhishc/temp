version: "3.9"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    image: temp-app:latest
    environment:
      NODE_ENV: development
      PORT: 3000
      HEALTHCHECK_PATH: /health
    ports:
      - "3000:3000"
    command: npm start
    init: true
